# Manuscript Tables

Retained main-text tables:

- `tableA_network_endpoints.*`: endpoint/data-residency summary, with file-level audit details left to the supplement.
- `table2_executable_adapters.*`: executable in-browser adapters and pinned runtimes.
- `tableB_qc_thresholds.*`: principal QC review defaults; full source-level registry remains in the experiment artifacts.

Supplementary tables added for reviewer reconciliation:

- `tableD_tool_options.*`: package function calls, fixed comparison arguments, and package-default arguments.
- `tableE_maf_excluded_events.*`: reason-level reconciliation of the 231 example-MAF rows excluded from the SBS matrices.

The per-experiment E1/E2/E3/E4/E6 tables were retired because they duplicate the manuscript figures and the machine-readable experiment/supplement files. The full QC threshold registry remains under `docs/manuscript/experiments/qc_threshold_registry/`.
