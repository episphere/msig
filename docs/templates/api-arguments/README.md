# API Argument Templates

These HTML fragments support legacy API argument examples. They are kept outside the repository root so the public SDK entrypoints and reproducibility folders remain easy to scan.
